version: "3.4"
networks: 
 pihole_net: 
    driver: bridge
    driver_opts:
        com.docker.network.bridge.host_binding_ipv4: 10.2.3.12
        com.docker.network.bridge.name: "pihole"
        com.docker.network.bridge.enable_ip_masquerade: "true"
# More info at https://github.com/pi-hole/docker-pi-hole/ and https://docs.pi-hole.net/
services:
  pihole:
    container_name: pihole
    image: pihole/pihole:latest
    networks: 
      - pihole_net
    ports:
      - "53:53/tcp"
      - "53:53/udp"
      - "67:67/udp"
      - "80:80/tcp"
    environment:
      TZ: 'Australia/Sydney'
      WEBPASSWORD: 'Rise-Relic-Antennae6'
    # Volumes store your data between container upgrades
    volumes:
      - '/home/ubuntu/dockervol/pihole/etc-pihole/:/etc/pihole/'
      - '/home/ubuntu/dockervol/pihole/etc-dnsmasq.d/:/etc/dnsmasq.d/'
    # Recommended but not required (DHCP needs NET_ADMIN)
    #   https://github.com/pi-hole/docker-pi-hole#note-on-capabilities
    cap_add:
      - NET_ADMIN
    restart: unless-stopped